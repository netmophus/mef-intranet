/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Config par défaut. L'API est appelée en cross-origin (voir src/lib/api.js),
     avec l'hôte dérivé de l'intranet pour rester same-site. */
};

export default nextConfig;
